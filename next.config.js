/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        domains:["image.dummyjson.com","oweek-bundling-profile-img.s3.ap-southeast-3.amazonaws.com"]
    },
}

module.exports = nextConfig
